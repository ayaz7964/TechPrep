var { topic } = require('./helpers');

module.exports = [

  // ==============================================================
  // Kubernetes Architecture
  // ==============================================================
  topic('k8s-architecture', 'Kubernetes Architecture', 'beginner', 15,
    'Kubernetes architecture is like the operating system for a cluster of computers. Just as an OS manages processes, memory, and devices on a single machine, Kubernetes manages containers across many machines. It has a brain called the Control Plane that makes decisions, and workers called Nodes that run the actual applications. The Control Plane components (API Server, Scheduler, Controller Manager, etcd) coordinate everything, while each Node runs a kubelet agent that follows the Control Plane instructions.',
    [
      'Kubernetes follows a master-worker architecture with a Control Plane and Worker Nodes forming the cluster backbone',
      'The Control Plane runs global decisions — scheduling, scaling, responding to cluster events via controllers',
      'Worker Nodes host Pods and communicate with the Control Plane through the kubelet agent',
      'etcd stores the entire cluster state as a distributed key-value store, ensuring consistency and high availability',
      'The API Server is the single entry point for all cluster operations, exposing REST endpoints for users and components'
    ],
    [
      { heading: 'Control Plane Components', text: 'The Control Plane consists of several components: kube-apiserver exposes the Kubernetes API, kube-scheduler assigns Pods to Nodes, kube-controller-manager runs controllers that handle routine tasks (node health, replication, endpoint management), and etcd stores cluster state. These components can run on dedicated master nodes or be deployed for high availability across multiple machines.' },
      { heading: 'Worker Node Architecture', text: 'Each Worker Node runs three essential components: kubelet (the primary node agent that registers the node with the cluster and manages Pods), kube-proxy (maintains network rules for Service connectivity), and a container runtime (like containerd or CRI-O) that pulls images and runs containers. Nodes report their status and resource usage back to the Control Plane periodically.' },
      { heading: 'Declarative Model and Reconciliation', text: 'Kubernetes uses a declarative model — you specify the desired state (e.g., "run 3 replicas of nginx") in YAML or JSON, and controllers continuously reconcile actual state toward desired state. If a Pod crashes, the ReplicaSet controller detects the mismatch and creates a replacement. This self-healing mechanism is the core architectural pattern that makes Kubernetes resilient.' }
    ],
    [
      { question: 'What are the main components of Kubernetes architecture?', answer: 'Kubernetes architecture has two main parts: Control Plane (API Server, Scheduler, Controller Manager, etcd) and Worker Nodes (kubelet, kube-proxy, container runtime). The Control Plane manages the cluster, while Nodes run the actual workloads.' },
      { question: 'What is the role of kube-apiserver?', answer: 'The API Server is the front-end of the Control Plane. It validates and processes RESTful API requests, authenticates users, and persists cluster state to etcd. Every interaction with Kubernetes goes through the API Server.' },
      { question: 'How does kube-scheduler work?', answer: 'The Scheduler watches for newly created Pods and assigns them to suitable Nodes based on resource requirements, constraints, affinity rules, and data locality. It considers factors like CPU/memory availability, taints/tolerations, and pod affinity.' },
      { question: 'What is etcd and why is it important?', answer: 'etcd is a distributed, consistent key-value store that holds the entire cluster state. It uses the Raft consensus algorithm for reliability. If etcd goes down, the cluster cannot reconcile state changes, though existing workloads continue running.' },
      { question: 'What does kubelet do on each node?', answer: 'Kubelet is the primary node agent that registers the node, watches for Pod assignments from the API Server, ensures containers are running and healthy, and reports node/Pod status back to the Control Plane.' },
      { question: 'How does Kubernetes handle networking between Pods?', answer: 'Kubernetes requires a flat network where every Pod gets a unique IP and can communicate with any other Pod without NAT. kube-proxy on each node manages iptables/IPVS rules for Service traffic routing, and CNI plugins (Calico, Flannel) implement the actual network fabric.' },
      { question: 'What is the role of kube-controller-manager?', answer: 'The Controller Manager runs various controllers that watch cluster state and make changes to drive actual state toward desired state. Key controllers include Node Controller, Replication Controller, Endpoint Controller, and Service Account Controller.' },
      { question: 'How does Kubernetes achieve high availability?', answer: 'HA is achieved by running multiple Control Plane replicas (multiple API Servers, Scheduler, Controller Manager with leader election) and a multi-node etcd cluster. Worker Nodes can scale horizontally, and workloads use multiple replicas distributed across nodes.' },
      { question: 'What is a container runtime in Kubernetes?', answer: 'The container runtime is the software that actually runs containers. Kubernetes uses the CRI (Container Runtime Interface) to support multiple runtimes. Common choices are containerd, CRI-O, and Docker (via cri-dockerd).' },
      { question: 'Explain the difference between kubectl and kubeadm.', answer: 'kubectl is the CLI tool for interacting with the Kubernetes API Server (used daily by developers and admins). kubeadm is a bootstrap tool for setting up and joining Kubernetes clusters (used during cluster initialization and node joining).' }
    ],
    [
      { question: 'What is the primary role of the Kubernetes Control Plane?', options: ['Run application containers', 'Manage cluster state and orchestration', 'Store application data', 'Provide load balancing'], answer: 1, explanation: 'The Control Plane manages the cluster state, makes global decisions, and orchestrates all activities within the cluster.' },
      { question: 'Which component stores the cluster state in Kubernetes?', options: ['kube-apiserver', 'kube-scheduler', 'etcd', 'kube-controller-manager'], answer: 2, explanation: 'etcd is a distributed key-value store that stores the entire cluster configuration and state persistently.' },
      { question: 'What is the function of kube-scheduler?', options: ['Expose the Kubernetes API', 'Assign Pods to Nodes', 'Run controllers', 'Store cluster state'], answer: 1, explanation: 'The scheduler watches for unscheduled pods and assigns them to appropriate nodes based on constraints and resources.' },
      { question: 'Which component runs on every Worker Node?', options: ['etcd', 'kube-controller-manager', 'kubelet', 'kube-scheduler'], answer: 2, explanation: 'Kubelet runs on every node and is responsible for managing Pods and containers on that node.' },
      { question: 'What does kube-proxy handle?', options: ['Container runtime', 'Network rules for Services', 'Pod scheduling', 'Cluster state storage'], answer: 1, explanation: 'kube-proxy maintains network rules on each node, implementing Service-to-Pod traffic routing using iptables or IPVS.' },
      { question: 'Which protocol does Kubernetes use for container runtime communication?', options: ['OCI', 'CRI', 'CNI', 'CSI'], answer: 1, explanation: 'CRI (Container Runtime Interface) is the plugin interface that allows kubelet to use different container runtimes without recompilation.' },
      { question: 'How does Kubernetes achieve self-healing?', options: ['Manual restart', 'Controllers reconcile desired vs actual state', 'kube-proxy detects failures', 'etcd triggers recovery'], answer: 1, explanation: 'Controllers continuously watch cluster state and make changes to match the desired state, automatically recovering from failures.' },
      { question: 'What is the default container runtime for modern Kubernetes?', options: ['Docker', 'containerd', 'rkt', 'hyperkit'], answer: 1, explanation: 'containerd is the default CRI-compliant runtime for Kubernetes 1.24+, replacing Docker as the default.' },
      { question: 'Which tool is used to bootstrap a Kubernetes cluster?', options: ['kubectl', 'kubeadm', 'helm', 'minikube'], answer: 1, explanation: 'kubeadm is the official tool for bootstrapping production-ready Kubernetes clusters.' },
      { question: 'What happens if etcd becomes unavailable?', options: ['Cluster stops completely', 'Existing workloads continue but no state changes', 'All Pods are terminated', 'New Pods are created automatically'], answer: 1, explanation: 'Existing workloads continue running, but the cluster cannot reconcile state, schedule new Pods, or respond to changes until etcd recovers.' }
    ],
    [
      { title: 'Deploy a Pod with kubectl run', useCase: 'Quickly start a single Pod for testing', code: 'kubectl run nginx-pod --image=nginx --restart=Never', description: 'Creates a single Pod named nginx-pod running the nginx image.' },
      { title: 'View Cluster Information', useCase: 'Check cluster health and component status', code: 'kubectl cluster-info\nkubectl get componentstatuses', description: 'Displays the cluster Control Plane endpoints and component health.' },
      { title: 'Get Node Details', useCase: 'Inspect worker node resources and status', code: 'kubectl get nodes -o wide\nkubectl describe node <node-name>', description: 'Lists all nodes with their status, roles, and resource capacity.' },
      { title: 'Watch API Server Events', useCase: 'Debug scheduling or resource issues', code: 'kubectl get events --all-namespaces --watch', description: 'Streams events from the API Server showing scheduling decisions and failures.' },
      { title: 'Inspect Pod Logs from Control Plane', useCase: 'Debug Control Plane component issues', code: 'kubectl logs -n kube-system kube-apiserver-<node>\nkubectl describe pod -n kube-system etcd-<node>', description: 'View logs of Control Plane Pods in the kube-system namespace.' }
    ]
  ),

  // ==============================================================
  // Cluster
  // ==============================================================
  topic('k8s-cluster', 'Cluster', 'beginner', 10,
    'A Kubernetes cluster is a group of computers that work together as a single powerful unit. Think of it like a sports team — you have a coach (Control Plane) who makes strategic decisions, and players (Worker Nodes) who execute the plays. The coach decides who plays when, and the players run the actual game. Together they form a cluster that can run applications reliably, scale up when demand increases, and heal itself when something breaks.',
    [
      'A Kubernetes cluster consists of at least one Control Plane node and multiple Worker Nodes working as a unified system',
      'The Control Plane manages cluster state, scheduling, and coordination while Worker Nodes host application workloads',
      'Clusters can span multiple physical or virtual machines across on-premises data centers or cloud providers',
      'Production clusters typically run multiple Control Plane replicas for high availability and fault tolerance',
      'Nodes within a cluster communicate via the Kubernetes API and a flat network fabric provided by CNI plugins'
    ],
    [
      { heading: 'Cluster Topology and Node Roles', text: 'A cluster has distinct node roles: Control Plane nodes run etcd, API Server, Scheduler, and Controller Manager. Worker Nodes run kubelet, kube-proxy, and the container runtime. In production, you would have 3+ Control Plane nodes (for etcd quorum) and many Worker Nodes scaled based on workload demands. Cloud providers like EKS, AKS, and GKE manage the Control Plane for you.' },
      { heading: 'Cluster Networking Requirements', text: 'Every Pod must be able to communicate with every other Pod without NAT, every Node must be able to communicate with every Pod, and the network must provide a flat IP space. CNI plugins such as Calico (BGP-based), Flannel (VXLAN overlay), or Cilium (eBPF-based) implement these requirements by assigning unique IPs to Pods and managing routing rules across the cluster.' },
      { heading: 'Cluster Lifecycle and Maintenance', text: 'Clusters require ongoing maintenance: certificate rotation (kubelet and API Server certs expire after 1 year by default), version upgrades (Kubernetes releases quarterly), node patching (OS and runtime updates), and backup/restore of etcd data. Tools like kubeadm, kOps, and EKS managed services simplify cluster lifecycle management.' }
    ],
    [
      { question: 'What is a Kubernetes cluster?', answer: 'A Kubernetes cluster is a set of machines (nodes) that run containerized applications managed by Kubernetes. It includes a Control Plane for management and Worker Nodes for running workloads.' },
      { question: 'What are the minimum requirements for a cluster?', answer: 'A minimal cluster needs at least one Control Plane node and one Worker Node. For production HA, you need at least 3 Control Plane nodes (for etcd quorum) and multiple Worker Nodes.' },
      { question: 'How do nodes register with a cluster?', answer: 'Nodes register by running kubelet with a join token or certificate signed by the cluster CA. Kubelet presents its credentials to the API Server, which authenticates and adds the node to the cluster.' },
      { question: 'What is a single-node cluster used for?', answer: 'Single-node clusters like minikube, kind, or k3s are used for local development, testing, and learning. They run all Control Plane and Worker components on one machine.' },
      { question: 'How does cluster scaling work?', answer: 'Cluster scaling adds or removes Worker Nodes. This can be manual (using kubeadm join/reset) or automatic via Cluster Autoscaler, which monitors pending Pods and adjusts node count accordingly.' },
      { question: 'What is the difference between a cluster and a namespace?', answer: 'A cluster is the entire Kubernetes environment. Namespaces are virtual clusters within a physical cluster — they isolate resources and provide scope for names, RBAC, and resource quotas.' },
      { question: 'How do you check cluster health?', answer: 'Use kubectl get componentstatuses to check Control Plane health, kubectl get nodes to view node status, and kubectl cluster-info for cluster endpoints.' },
      { question: 'What happens when a node fails?', answer: 'The Node Controller detects the failure after a timeout (default 40s). Pods on the failed node are evicted after 5 minutes (pod-eviction-timeout) and rescheduled on healthy nodes.' },
      { question: 'Can a cluster span multiple regions?', answer: 'While possible with advanced networking, it is not recommended due to latency and stateful workload constraints. Kubernetes assumes a flat, low-latency network. Multi-region clusters require service mesh or federation solutions.' },
      { question: 'What is the cluster CA and why is it important?', answer: 'The Cluster Certificate Authority (CA) signs all certificates within the cluster (API Server, kubelet, etcd, etc.). It is the root of trust for secure communication between components.' }
    ],
    [
      { question: 'What is the minimum number of Control Plane nodes recommended for production?', options: ['1', '3', '5', '7'], answer: 1, explanation: 'Production clusters need 3 Control Plane nodes for etcd quorum (requires majority, so N/2+1 nodes).' },
      { question: 'Which component handles node failure detection?', options: ['kube-scheduler', 'kube-controller-manager (Node Controller)', 'kubelet', 'kube-proxy'], answer: 1, explanation: 'The Node Controller within kube-controller-manager monitors node health via periodic heartbeats.' },
      { question: 'What is the default pod eviction timeout after node failure?', options: ['40 seconds', '5 minutes', '15 minutes', '30 seconds'], answer: 1, explanation: 'Pods on an unreachable node are evicted after 5 minutes by default (pod-eviction-timeout).' },
      { question: 'Which tool is used for local development clusters?', options: ['kubeadm', 'kubectl', 'minikube', 'etcdctl'], answer: 2, explanation: 'minikube runs a single-node Kubernetes cluster locally, ideal for development and testing.' },
      { question: 'What does the Cluster Autoscaler do?', options: ['Scale Pods up/down', 'Scale Nodes up/down', 'Scale storage', 'Scale network'], answer: 1, explanation: 'Cluster Autoscaler adjusts the number of Worker Nodes based on resource demands and pending Pods.' },
      { question: 'How do Worker Nodes communicate with the Control Plane?', options: ['Direct SSH', 'API Server', 'etcd directly', 'kube-proxy'], answer: 1, explanation: 'All components, including Worker Nodes, communicate with the cluster through the API Server.' },
      { question: 'What happens to Pods on a failed node?', options: ['They are restarted on the same node', 'They are deleted and rescheduled', 'They continue running', 'They are lost permanently'], answer: 1, explanation: 'After the eviction timeout, Pods are deleted from the API Server and rescheduled onto healthy nodes.' },
      { question: 'What is etcd quorum in a 3-node cluster?', options: ['1 node', '2 nodes', '3 nodes', 'All nodes'], answer: 1, explanation: 'etcd quorum requires a majority of nodes (N/2+1), so 2 out of 3 nodes must be healthy.' },
      { question: 'Which network requirement must Kubernetes clusters satisfy?', options: ['Pods must use host networking', 'Pods must communicate without NAT', 'All traffic must go through Ingress', 'Services must have external IPs'], answer: 1, explanation: 'Kubernetes requires that every Pod can communicate with every other Pod without network address translation.' },
      { question: 'What manages Pod-to-Pod networking across nodes?', options: ['kube-proxy', 'CNI plugin', 'kubelet', 'etcd'], answer: 1, explanation: 'CNI plugins (Calico, Flannel, Cilium) implement the cluster network fabric that enables Pod-to-Pod communication across nodes.' }
    ],
    [
      { title: 'Create a kind Cluster', useCase: 'Spin up a local multi-node cluster for testing', code: 'kind create cluster --name demo --config - <<EOF\nkind: Cluster\napiVersion: kind.x-k8s.io/v1alpha4\nnodes:\n- role: control-plane\n- role: worker\n- role: worker\nEOF', description: 'Creates a 3-node kind cluster (1 Control Plane + 2 Workers) locally using Docker.' },
      { title: 'Check Cluster Info', useCase: 'Verify cluster connection and endpoints', code: 'kubectl cluster-info\nkubectl cluster-info dump', description: 'Shows cluster endpoints and dumps cluster diagnostics.' },
      { title: 'List All Nodes', useCase: 'View node status, roles, and versions', code: 'kubectl get nodes -o wide\nkubectl describe node <node-name>', description: 'Lists all nodes with internal IP, roles, and Kubernetes version.' },
      { title: 'Get Cluster Events', useCase: 'Troubleshoot cluster-level issues', code: 'kubectl get events --all-namespaces --sort-by=.lastTimestamp', description: 'Shows all events across the cluster, sorted by time.' },
      { title: 'Drain a Node for Maintenance', useCase: 'Safely evict Pods before node maintenance', code: 'kubectl drain <node-name> --ignore-daemonsets --delete-emptydir-data\nkubectl cordon <node-name>', description: 'Safely evicts all Pods from a node and marks it unschedulable for maintenance.' }
    ]
  ),

  // ==============================================================
  // Control Plane
  // ==============================================================
  topic('k8s-control-plane', 'Control Plane', 'beginner', 10,
    'The Control Plane is the brain of the Kubernetes cluster. It makes all global decisions — what to run, where to run it, and how to keep everything healthy. You can think of it like an air traffic control tower: it sees all the planes (Pods), knows where they should go, and coordinates their movements. The Control Plane includes the API Server (the communication hub), Scheduler (who goes where), Controller Manager (watching and fixing), and etcd (remembering everything).',
    [
      'The Control Plane manages the cluster by making global decisions about scheduling, scaling, and responding to failures',
      'Core components: kube-apiserver (REST API gateway), kube-scheduler (Pod placement), kube-controller-manager (reconciliation loops), and etcd (state storage)',
      'Control Plane components are typically run on dedicated master nodes to isolate them from application workloads',
      'Production deployments run multiple Control Plane replicas with leader election for high availability',
      'Cloud-managed Kubernetes (EKS, AKS, GKE) abstracts the Control Plane, letting users focus on application workloads'
    ],
    [
      { heading: 'Control Plane Components in Detail', text: 'The API Server (kube-apiserver) is the front door — every CLI, dashboard, and automation tool talks to it. The Scheduler (kube-scheduler) watches for unscheduled Pods and picks the best Node using filtering (predicates) and scoring (priorities). The Controller Manager runs separate controller processes (Node Controller, Replication Controller, Endpoint Controller, etc.) in a single binary. etcd stores all configuration, state, and metadata as key-value pairs with watch support.' },
      { heading: 'High Availability Control Plane', text: 'For production, run 3 or 5 Control Plane nodes. API Servers sit behind a load balancer. Scheduler and Controller Manager use leader election — only one instance is active at a time. etcd runs as a Raft cluster requiring majority (N/2+1) nodes for writes. This setup survives individual node failures without cluster downtime.' },
      { heading: 'Securing the Control Plane', text: 'Control Plane access is secured through TLS certificates (mutual TLS between components), RBAC policies (who can do what), Service Accounts (Pod identity), and admission controllers (enforce policies before object creation). The API Server can integrate with external identity providers (OIDC, LDAP) for authentication.' }
    ],
    [
      { question: 'What components make up the Control Plane?', answer: 'The Control Plane consists of kube-apiserver, kube-scheduler, kube-controller-manager, and etcd. Together they manage cluster state, scheduling, and orchestration.' },
      { question: 'Can the Control Plane run on Worker Nodes?', answer: 'Yes, in small clusters (e.g., kubeadm) the Control Plane can run on the same nodes as workloads. For production, dedicated master nodes are recommended to avoid resource contention.' },
      { question: 'What happens if the Control Plane goes down?', answer: 'Existing workloads continue running on Worker Nodes, but you cannot deploy new Pods, scale, update, or respond to failures. The cluster becomes frozen until the Control Plane recovers.' },
      { question: 'How does the Control Plane scale?', answer: 'Control Plane components scale vertically (more resources per instance) rather than horizontally. However, you run multiple copies for HA with the active instance handling requests.' },
      { question: 'What is the difference between kube-controller-manager and cloud-controller-manager?', answer: 'kube-controller-manager runs controllers that are Kubernetes-core (Node, Replication, Endpoint). cloud-controller-manager runs cloud-specific controllers (Load Balancers, Routes, Volumes) integrating with providers like AWS, Azure, GCP.' },
      { question: 'How does the Control Plane handle authentication?', answer: 'The API Server authenticates all requests using client certificates, bearer tokens, or OIDC integration. It then authorizes via RBAC or ABAC, and applies admission controllers before persisting objects.' },
      { question: 'What ports does the Control Plane use?', answer: 'Key ports: 6443 (API Server HTTPS), 2379-2380 (etcd client/peer), 10251 (Scheduler), 10252 (Controller Manager), 10257-10259 (secure ports for Scheduler and Controller Manager in newer versions).' },
      { question: 'How do you upgrade the Control Plane?', answer: 'Upgrade one node at a time: drain the node, upgrade kubeadm/kubelet, upgrade kubectl, uncordon. For HA clusters, upgrade nodes sequentially to maintain quorum. kubeadm upgrade plan shows upgrade path.' },
      { question: 'What is an admission controller?', answer: 'Admission controllers intercept requests to the API Server after authentication/authorization but before object persistence. They can validate (e.g., PodSecurity admission) or mutate (e.g., DefaultStorageClass admission) objects.' },
      { question: 'How does leader election work in the Control Plane?', answer: 'Scheduler and Controller Manager use leader election via Kubernetes Endpoints or ConfigMaps with leases. Only the elected leader performs active work. If the leader fails, another instance takes over within seconds.' }
    ],
    [
      { question: 'Which Control Plane component is the single entry point for all API requests?', options: ['etcd', 'kube-scheduler', 'kube-apiserver', 'kube-controller-manager'], answer: 2, explanation: 'The API Server is the front-end that validates and processes all API requests to the cluster.' },
      { question: 'How many Control Plane nodes are recommended for production HA?', options: ['1', '2', '3 or 5', '10'], answer: 2, explanation: '3 or 5 Control Plane nodes provide etcd quorum and fault tolerance at reasonable operational cost.' },
      { question: 'What port does the API Server listen on by default?', options: ['8080', '6443', '443', '8443'], answer: 1, explanation: 'The API Server listens on port 6443 for HTTPS traffic by default.' },
      { question: 'Which component performs leader election in the Control Plane?', options: ['kube-apiserver only', 'kube-scheduler and kube-controller-manager', 'etcd', 'All components'], answer: 1, explanation: 'Scheduler and Controller Manager use leader election to ensure only one active instance performs work.' },
      { question: 'What is the role of an admission controller?', options: ['Authenticate users', 'Intercept and validate/mutate API requests', 'Schedule Pods', 'Store cluster state'], answer: 1, explanation: 'Admission controllers intercept API requests after authentication to validate or mutate them before persistence.' },
      { question: 'Which Control Plane component stores cluster state?', options: ['kube-apiserver', 'kube-scheduler', 'etcd', 'kube-controller-manager'], answer: 2, explanation: 'etcd is the distributed key-value store that persists all cluster state and configuration.' },
      { question: 'What happens to existing workloads if the Control Plane fails?', options: ['Workloads stop immediately', 'Workloads continue running', 'Workloads are migrated', 'Workloads restart'], answer: 1, explanation: 'Existing workloads continue running on Worker Nodes, but no new changes can be made until the Control Plane recovers.' },
      { question: 'What is the function of cloud-controller-manager?', options: ['Manage cloud-specific controllers', 'Schedule Pods', 'Store cluster state', 'Run container runtime'], answer: 0, explanation: 'cloud-controller-manager runs cloud provider-specific controllers for load balancers, storage, and routing.' },
      { question: 'Which component selects the best Node for a Pod?', options: ['kube-apiserver', 'kube-scheduler', 'kube-controller-manager', 'kubelet'], answer: 1, explanation: 'The scheduler watches for unscheduled Pods and selects the optimal node for each Pod.' },
      { question: 'What is the default etcd port for peer communication?', options: ['2379', '2380', '6443', '10250'], answer: 1, explanation: 'etcd uses port 2380 for peer-to-peer communication between etcd cluster members.' }
    ],
    [
      { title: 'Check Control Plane Status', useCase: 'Verify all Control Plane components are healthy', code: 'kubectl get componentstatuses\nkubectl get pods -n kube-system', description: 'Shows health of etcd, scheduler, and controller-manager.' },
      { title: 'View Control Plane Pods', useCase: 'Inspect Control Plane component logs and status', code: 'kubectl get pods -n kube-system | select-string -pattern "apiserver|etcd|scheduler|controller"', description: 'Lists Control Plane Pods in the kube-system namespace.' },
      { title: 'Check API Server Health', useCase: 'Quick health check of the API Server', code: 'kubectl get --raw=/healthz\nkubectl get --raw=/livez', description: 'Returns health and liveness probe results from the API Server.' },
      { title: 'View Scheduler Configuration', useCase: 'Check scheduler policies and profiles', code: 'kubectl describe configmap -n kube-system kube-scheduler\nkubectl get pods -n kube-system -l component=kube-scheduler -o yaml', description: 'Displays scheduler configuration and the running scheduler Pod spec.' },
      { title: 'Check etcd Cluster Health', useCase: 'Verify etcd cluster quorum and health', code: 'kubectl exec -n kube-system etcd-<node> -- etcdctl endpoint health --cluster\nkubectl exec -n kube-system etcd-<node> -- etcdctl member list', description: 'Executes etcdctl inside an etcd Pod to check cluster health and member list.' }
    ]
  ),

  // ==============================================================
  // Worker Node
  // ==============================================================
  topic('k8s-worker-node', 'Worker Node', 'beginner', 10,
    'A Worker Node is a computer in the cluster that actually runs your applications. If the Control Plane is the brain, Worker Nodes are the muscles. Each Worker Node has three essential components: kubelet (the node agent that follows Control Plane orders), kube-proxy (handles networking rules), and a container runtime (like containerd) that runs the actual containers. You can add or remove Worker Nodes to scale the cluster capacity up or down.',
    [
      'Worker Nodes host Pods and provide the compute, memory, and storage resources for application workloads',
      'Each Node runs three core components: kubelet, kube-proxy, and a container runtime (containerd/CRI-O)',
      'Nodes report their capacity, allocated resources, and health status to the Control Plane via kubelet heartbeats',
      'Worker Nodes can be physical servers, virtual machines, or cloud instances — Kubernetes abstracts the differences',
      'Node health is monitored by the Node Controller; unhealthy Nodes have their Pods evicted after a timeout'
    ],
    [
      { heading: 'Node Registration and Lifecycle', text: 'When a new Worker Node joins the cluster, kubelet registers itself with the API Server using a bootstrap token or certificate. The Node object appears in the cluster with labels (e.g., node-role, instance type, region) and taints. Administrators can cordon (mark unschedulable), drain (evict Pods), or delete nodes as needed. When a node is deleted, its Pods are rescheduled onto remaining nodes.' },
      { heading: 'Resource Management on Nodes', text: 'Each Node reports its total capacity (CPU cores, memory, max Pods). Kubelet manages resource reservations: system-reserved (for OS and system daemons) and kube-reserved (for kubelet, kube-proxy, container runtime). The rest is allocatable for Pods. Kubelet evicts Pods when resources run low, starting with lowest priority Pods when disk pressure, memory pressure, or PID pressure is detected.' },
      { heading: 'Node Conditions and Health', text: 'Kubelet periodically updates Node conditions: Ready (node is healthy), DiskPressure (disk space low), MemoryPressure (memory low), PIDPressure (too many processes), NetworkUnavailable (network not configured). If Ready becomes Unknown for more than 40 seconds, the Node Controller marks the node unreachable. After 5 minutes, Pods on the unreachable node are evicted.' }
    ],
    [
      { question: 'What components run on every Worker Node?', answer: 'Every Worker Node runs kubelet (node agent), kube-proxy (network proxy), and a container runtime (containerd, CRI-O, or others via CRI).' },
      { question: 'How does a node join a cluster?', answer: 'A node joins by running kubelet with a join token or bootstrap certificate. Kubelet presents credentials to the API Server, which authenticates and adds the Node object to the cluster.' },
      { question: 'What happens when a node runs out of memory?', answer: 'Kubelet starts evicting Pods based on QoS class: BestEffort Pods first, then Burstable, finally Guaranteed. The Pods are terminated and rescheduled on available nodes if possible.' },
      { question: 'What is the difference between cordon and drain?', answer: 'Cordon marks a node as unschedulable so no new Pods are assigned. Drain evicts existing Pods from the node before maintenance. Drain typically includes cordon as a first step.' },
      { question: 'How does kubelet ensure containers are healthy?', answer: 'Kubelet runs liveness probes (checks if container is alive — restart if fails), readiness probes (checks if container is ready to serve traffic — remove from Service if fails), and startup probes (for slow-starting containers).' },
      { question: 'What is a taint on a Worker Node?', answer: 'A taint is a label that repels Pods unless they have a matching toleration. For example, a GPU node might have a taint so that only GPU workloads are scheduled there.' },
      { question: 'How many Pods can a single node run?', answer: 'The default max is 110 Pods per node (configurable via kubelet --max-pods flag). Cloud providers often set lower limits (e.g., AWS EKS limits based on instance type ENI limits).' },
      { question: 'What is a static Pod?', answer: 'A static Pod is managed directly by kubelet from a manifest file on the node (usually in /etc/kubernetes/manifests/). kubelet watches this directory and creates/deletes Pods accordingly. Used for Control Plane components.' },
      { question: 'Can a node run different container runtimes?', answer: 'No, each node uses a single container runtime. The runtime is configured in kubelet via the --container-runtime flag. All Pods on that node use the same runtime.' },
      { question: 'How does node-to-node networking work?', answer: 'Nodes communicate through the cluster network fabric implemented by the CNI plugin. Each node gets an IP range (Pod CIDR) assigned, and the CNI plugin handles routing between these ranges across the cluster.' }
    ],
    [
      { question: 'What is the default maximum number of Pods per node?', options: ['50', '110', '250', '500'], answer: 1, explanation: 'The default max Pods per node in Kubernetes is 110, configurable via kubelet --max-pods.' },
      { question: 'Which component on the Worker Node manages containers?', options: ['kube-proxy', 'kubelet', 'container runtime', 'kube-scheduler'], answer: 1, explanation: 'The container runtime (containerd, CRI-O) handles pulling images and running containers under kubelet direction.' },
      { question: 'What does cordoning a node accomplish?', options: ['Deletes the node', 'Marks node unschedulable', 'Evicts all Pods', 'Restarts kubelet'], answer: 1, explanation: 'Cordoning marks a node unschedulable so no new Pods are scheduled onto it, while existing Pods continue running.' },
      { question: 'Which kubelet probe determines if a container should be restarted?', options: ['Readiness probe', 'Liveness probe', 'Startup probe', 'Health probe'], answer: 1, explanation: 'A failing liveness probe causes kubelet to restart the container. A failing readiness probe only removes traffic.' },
      { question: 'What triggers Pod eviction on a Worker Node?', options: ['API Server command', 'Resource pressure (memory/disk)', 'Scheduler decision', 'etcd state change'], answer: 1, explanation: 'Kubelet evicts Pods when it detects memory pressure, disk pressure, or PID pressure on the node.' },
      { question: 'Which component handles Service-to-Pod traffic on a node?', options: ['kubelet', 'kube-proxy', 'container runtime', 'kube-scheduler'], answer: 1, explanation: 'kube-proxy maintains iptables or IPVS rules on each node to route Service traffic to Pods.' },
      { question: 'What is the Node Ready condition?', options: ['Node is full', 'Node is healthy and ready', 'Node is shutting down', 'Node has new Pods'], answer: 1, explanation: 'The Ready condition indicates the Node is healthy and can accept new Pods.' },
      { question: 'How does a node get its Pod CIDR range?', options: ['CNI plugin assigns it', 'kubelet auto-generates', 'API Server assigns via node controller', 'Manual configuration'], answer: 2, explanation: 'The node controller assigns Pod CIDR ranges to nodes when using the Controller Manager with --allocate-node-cidrs=true.' },
      { question: 'What QoS class gets evicted first under memory pressure?', options: ['Guaranteed', 'Burstable', 'BestEffort', 'All equally'], answer: 2, explanation: 'BestEffort Pods (no resource requests/limits) are evicted first under resource pressure.' },
      { question: 'What is a static Pod?', options: ['A Pod managed by API Server', 'A Pod with no replicas', 'A Pod managed by kubelet from local manifests', 'A paused Pod'], answer: 2, explanation: 'Static Pods are created by kubelet from manifest files in /etc/kubernetes/manifests/, used for Control Plane components.' }
    ],
    [
      { title: 'Get Node Details', useCase: 'View worker node capacity and status', code: 'kubectl get node <node-name> -o wide\nkubectl describe node <node-name>', description: 'Shows node status, capacity, allocatable resources, conditions, and running Pods.' },
      { title: 'Taint a Node', useCase: 'Reserve a node for specific workloads', code: 'kubectl taint nodes <node-name> dedicated=gpu:NoSchedule\nkubectl describe node <node-name> | findstr Taints', description: 'Adds a taint that prevents Pods without matching toleration from scheduling on this node.' },
      { title: 'Cordon and Drain a Node', useCase: 'Prepare node for maintenance or decommission', code: 'kubectl cordon <node-name>\nkubectl drain <node-name> --ignore-daemonsets --delete-emptydir-data', description: 'Marks node unschedulable and evicts Pods for maintenance.' },
      { title: 'Label a Node', useCase: 'Add custom labels for node selection', code: 'kubectl label nodes <node-name> topology.kubernetes.io/zone=us-east-1a\nkubectl get nodes --show-labels', description: 'Adds labels that can be used for nodeSelector, affinity, or topology constraints.' },
      { title: 'Check Node Resource Usage', useCase: 'Monitor node CPU/memory utilization', code: 'kubectl top node <node-name>\nkubectl describe node <node-name> | findstr -A5 "Allocated resources"', description: 'Shows current resource usage and allocated resources on the node.' }
    ]
  ),

  // ==============================================================
  // kube-apiserver
  // ==============================================================
  topic('k8s-apiserver', 'kube-apiserver', 'intermediate', 15,
    'The kube-apiserver is the front door to the entire Kubernetes cluster. Every single operation — whether from kubectl, the dashboard, CI/CD pipelines, or other components — goes through the API Server. It validates requests, authenticates users, checks permissions, applies admission control policies, and persists the final state to etcd. Think of it as the customer service desk of a large organization: it routes every request to the right place and records everything.',
    [
      'The API Server is the only component that directly interacts with etcd — all other components communicate through it',
      'It handles authentication (who you are), authorization (what you can do), and admission control (what policies apply)',
      'The API Server exposes a RESTful API that supports CRUD operations on all Kubernetes resources (Pods, Services, Deployments, etc.)',
      'It implements watch semantics — clients can subscribe to changes and receive real-time updates without polling',
      'The API Server is horizontally scalable: multiple instances can run behind a load balancer for high availability'
    ],
    [
      { heading: 'Request Flow Through the API Server', text: 'When a request arrives, the API Server goes through a pipeline: 1) Authentication — verifies identity via TLS client certificates, bearer tokens, or OIDC. 2) Authorization — checks RBAC rules to determine if the user can perform the action. 3) Admission — runs a chain of admission controllers that can validate or mutate the request. 4) Validation — validates the object schema and business rules. 5) Persistence — stores the object in etcd and returns the response.' },
      { heading: 'API Groups and Versioning', text: 'Kubernetes API is organized into API groups (e.g., apps, networking.k8s.io, batch). Each group has versions (v1, v1beta1, v1beta2) following a lifecycle: alpha (disabled by default, may be removed), beta (enabled by default, well-tested), stable (GA, long-term support). The API Server handles conversion between versions transparently, allowing you to use any supported version in your manifests.' },
      { heading: 'Watch Mechanism and Event-Driven Architecture', text: 'The API Server implements a watch mechanism based on etcd watches. Clients (like controllers, kubelet, kube-proxy) can establish a long-lived HTTP connection to watch for changes on specific resources. When a resource changes, the API Server streams the event (ADDED, MODIFIED, DELETED) to all watchers. This enables the event-driven, reactive behavior that powers Kubernetes controllers without polling.' }
    ],
    [
      { question: 'What is the primary function of kube-apiserver?', answer: 'The API Server is the central hub that exposes the Kubernetes API, validates and processes all requests, enforces authentication/authorization/admission, and persists cluster state to etcd.' },
      { question: 'How does the API Server authenticate requests?', answer: 'It supports multiple authentication methods: TLS client certificates (X.509), bearer tokens (static, service account, bootstrap), OpenID Connect (OIDC integration with providers like Google, Azure AD), and webhook token authentication.' },
      { question: 'What is an API group in Kubernetes?', answer: 'API groups organize related resources. Core group (no path, e.g., /api/v1), named groups (e.g., /apis/apps/v1 for Deployments). Groups help with API evolution and clean resource organization.' },
      { question: 'How does the API Server handle versioning?', answer: 'Different versions (v1, v1beta1, v1beta2) coexist. The API Server converts between versions automatically. Users can submit any supported version, and storage is in the preferred version.' },
      { question: 'What happens when the API Server is overloaded?', answer: 'It throttles requests based on --max-requests-inflight and --max-mutating-requests-inflight. Priority and fairness (API Priority and Fairness) ensure critical requests (like kubelet heartbeats) are not starved by less important requests.' },
      { question: 'How does the API Server ensure data consistency?', answer: 'It uses optimistic concurrency — every resource has a resourceVersion field (etcd modification index). When updating, the API Server checks the version matches; if not, it returns a 409 Conflict error, and the client must re-fetch and retry.' },
      { question: 'What is an admission webhook?', answer: 'An admission webhook is an HTTP callback that receives admission requests and returns admission responses. MutatingWebhookConfiguration can modify objects, ValidatingWebhookConfiguration can reject them. Used for policy enforcement (OPA, Kyverno) and default value injection.' },
      { question: 'How does the API Server integrate with RBAC?', answer: 'The API Server checks authorization for every request using SubjectAccessReview. RBAC is defined by Roles (permissions within a namespace) and ClusterRoles (cluster-wide), bound to users/groups/ServiceAccounts via RoleBinding and ClusterRoleBinding.' },
      { question: 'What is an APIService resource?', answer: 'APIService is a resource for registering API extensions. It allows custom API servers (like metrics-server, service-catalog) to be aggregated into the main API Server, making their resources available through kubectl and the usual API.' },
      { question: 'How does the API Server handle DELETE operations?', answer: 'DELETE can be immediate (force delete) or graceful with a grace period. For graceful deletion, the object gets a deletionTimestamp, and finalizers can block deletion until cleanup completes. After finalizers finish, the object is removed.' }
    ],
    [
      { question: 'Which component directly writes to etcd?', options: ['kube-scheduler', 'kube-apiserver', 'kube-controller-manager', 'kubelet'], answer: 1, explanation: 'Only the API Server interacts directly with etcd. All other components go through the API Server.' },
      { question: 'What authentication method does kube-apiserver NOT support?', options: ['X.509 client certificates', 'Bearer tokens', 'LDAP passwords', 'OIDC tokens'], answer: 2, explanation: 'The API Server does not support LDAP password authentication directly. It supports OIDC which can integrate with LDAP directories.' },
      { question: 'What API version suffix indicates alpha features?', options: ['v1', 'v1beta1', 'v1alpha1', 'v1rc1'], answer: 2, explanation: 'Alpha versions (v1alpha1, v1alpha2) have the alpha suffix and may be disabled by default or removed.' },
      { question: 'What HTTP status code indicates a conflict due to stale resourceVersion?', options: ['400', '401', '409', '412'], answer: 2, explanation: 'A 409 Conflict is returned when the resourceVersion in the update does not match the current version.' },
      { question: 'Which mechanism allows clients to get real-time updates from the API Server?', options: ['Polling', 'WebSocket', 'Watch', 'Pub/Sub'], answer: 2, explanation: 'The Watch mechanism streams ADDED, MODIFIED, DELETED events to clients via long-lived HTTP connections.' },
      { question: 'What is a MutatingWebhookConfiguration used for?', options: ['Block requests', 'Modify requests before storage', 'Authenticate users', 'Log requests'], answer: 1, explanation: 'Mutating admission webhooks can modify incoming API objects before they are persisted.' },
      { question: 'How does API Priority and Fairness help?', options: ['Increases API Server speed', 'Prevents lower-priority requests from starving critical ones', 'Caches responses', 'Load balances requests'], answer: 1, explanation: 'Priority and Fairness ensures critical system requests (like kubelet heartbeats) are handled even under load.' },
      { question: 'What resource registers an aggregated API server?', options: ['APIService', 'CustomResourceDefinition', 'AggregatedAPI', 'APIExtension'], answer: 0, explanation: 'APIService registers external API servers that extend the Kubernetes API.' },
      { question: 'What blocks object deletion until cleanup is done?', options: ['Admission controllers', 'Finalizers', 'Resource versions', 'Owner references'], answer: 1, explanation: 'Finalizers specify pre-delete cleanup tasks. The object is not deleted until all finalizers are removed.' },
      { question: 'Which API Server flag limits concurrent non-mutating requests?', options: ['--max-mutating-requests-inflight', '--max-requests-inflight', '--max-concurrent-requests', '--api-rate-limit'], answer: 1, explanation: '--max-requests-inflight limits the number of concurrent non-mutating requests to the API Server.' }
    ],
    [
      { title: 'Call the API Server Directly', useCase: 'Test API access and troubleshoot authentication', code: 'kubectl get --raw=/api/v1/namespaces/default/pods\nkubectl get --raw=/openapi/v2 | findstr "info"', description: 'Direct REST call to the API Server listing Pods, and inspecting the OpenAPI spec.' },
      { title: 'Test API Server Auth with curl', useCase: 'Verify API Server authentication from outside', code: 'curl -k --cert client.crt --key client.key https://<API-SERVER>:6443/api/v1/namespaces/default/pods', description: 'Uses client certificate authentication to directly query the API Server.' },
      { title: 'Enable API Server Audit Logging', useCase: 'Track all API requests for security auditing', code: 'kubectl logs -n kube-system kube-apiserver-<node> | Out-File apiserver.log', description: 'View API Server logs including audit events showing who did what.' },
      { title: 'Check API Server Health Endpoints', useCase: 'Troubleshoot API Server responsiveness', code: 'kubectl get --raw=/healthz\nkubectl get --raw=/readyz\nkubectl get --raw=/livez', description: 'Multiple health check endpoints providing detailed component status.' },
      { title: 'Explore API Resources', useCase: 'List available API versions and resources', code: 'kubectl api-versions\nkubectl api-resources --namespaced=true\nkubectl explain pod', description: 'Lists all API versions and available resources in the cluster.' }
    ]
  ),

  // ==============================================================
  // kube-scheduler
  // ==============================================================
  topic('k8s-scheduler', 'kube-scheduler', 'intermediate', 15,
    'The kube-scheduler decides which Node should run each new Pod. It is like a hotel manager deciding which room to assign to each guest based on their requirements and room availability. The scheduler looks at every unscheduled Pod, checks which Nodes meet its requirements (enough CPU/memory, right labels, no conflicting taints), then picks the best Node using a scoring system. Modern Kubernetes uses scheduling profiles and plugins to make this process customizable.',
    [
      'The scheduler watches the API Server for newly created Pods with an empty nodeName field and assigns them to optimal Nodes',
      'Scheduling is a two-phase process: Filtering (predicates — which Nodes can run the Pod) and Scoring (priorities — which Node is the best fit)',
      'The scheduler respects constraints: resource requests/limits, nodeSelector, affinity/anti-affinity, taints/tolerations, and topology spread constraints',
      'Each Pod can define its own scheduling constraints via nodeSelector, node affinity, pod affinity/anti-affinity, and tolerations',
      'Kubernetes supports multiple scheduling profiles, custom schedulers, and the scheduler extender mechanism for specialized scheduling needs'
    ],
    [
      { heading: 'Scheduling Cycle: Filtering and Scoring', text: 'The scheduling cycle has two stages. Filtering: the scheduler evaluates predicates like PodFitsResources (CPU/memory), PodFitsHost (nodeSelector), PodToleratesNodeTaints (tolerations), PodFitsHostPorts (port conflicts), and CheckNodeCondition (node ready). Scoring: the remaining nodes are scored on priorities like LeastRequestedPriority (spread load), BalancedResourceAllocation (balance CPU/memory), and SelectorSpreadPriority (zone spreading). The node with the highest score wins.' },
      { heading: 'Scheduling Profiles and Plugins', text: 'Kubernetes 1.19+ introduced the scheduler framework with pluggable scheduling profiles. You can configure multiple profiles (e.g., default-profile, gpu-profile) with different plugin sets. Plugins implement extension points: QueueSort (queue ordering), PreFilter/Filter (predicates), PreScore/Score (priorities), Reserve (resource reservation), Permit (wait for approval), and Bind (assign node). This allows custom scheduling logic without writing a standalone scheduler.' },
      { heading: 'Pod Priorities and Preemption', text: 'Pods can have priority classes (PriorityClass resources). Higher-priority Pods are scheduled first. If a high-priority Pod cannot be scheduled, the scheduler can preempt (evict) lower-priority Pods to free resources. Preemption follows rules: never preempt Pods in the same priority class, respect PodDisruptionBudgets, and minimize disruption by preempting the fewest/lowest-priority Pods needed.' }
    ],
    [
      { question: 'What is the kube-scheduler responsible for?', answer: 'The scheduler is responsible for assigning newly created Pods to appropriate Worker Nodes based on resource requirements, constraints, and policies.' },
      { question: 'What happens if no scheduler is running?', answer: 'Pods remain in Pending state indefinitely because their nodeName is never set. The cluster cannot assign Pods to Nodes without a scheduler.' },
      { question: 'How does the scheduler handle resource requirements?', answer: 'It checks that the Node has enough allocatable resources (CPU, memory, extended resources) to satisfy the Pod container requests. If requests are not set, the Pod can still be scheduled but may be evicted under pressure.' },
      { question: 'What is the difference between nodeSelector and node affinity?', answer: 'nodeSelector is a simple label match (must match all labels). Node affinity supports more flexible expressions: requiredDuringSchedulingIgnoredDuringExecution (hard requirement) and preferredDuringSchedulingIgnoredDuringExecution (soft preference with weight).' },
      { question: 'How does the scheduler handle Pod affinity?', answer: 'Pod affinity attracts Pods to Nodes that already run certain Pods (e.g., co-locate frontend with backend). Pod anti-affinity spreads Pods across Nodes (e.g., anti-affinity for availability zone). Both support required and preferred constraints.' },
      { question: 'What is pod preemption?', answer: 'When a high-priority Pod cannot be scheduled, the scheduler can evict (preempt) lower-priority Pods from Nodes to free resources for the high-priority Pod. The preempted Pods are gracefully terminated.' },
      { question: 'Can you run multiple schedulers in a cluster?', answer: 'Yes. You can deploy a custom scheduler alongside the default scheduler. Pods specify which scheduler to use via the schedulerName field. Different schedulers can use different policies and algorithms.' },
      { question: 'How does the scheduler handle topology spread?', answer: 'TopologySpreadConstraints spread Pods across topology domains (zones, nodes) to achieve high availability. You specify maxSkew (maximum difference in Pod count between domains), topologyKey (domain label), and whenUnsatisfiable (ScheduleAnyway or DoNotSchedule).' },
      { question: 'What scheduling plugins are available by default?', answer: 'Default plugins include NodeResourcesFit, NodeName, NodeUnschedulable, NodeAffinity, TaintToleration, PodTopologySpread, InterPodAffinity, VolumeBinding, and ImageLocality. Each plugin implements one or more extension points.' },
      { question: 'How does the scheduler handle volumes?', answer: 'The VolumeBinding plugin handles Pods that use PVCs. For immediate binding, it waits for PV availability. For WaitForFirstConsumer binding, it first considers the Pod constraints and then schedules to make the volume available on that node.' }
    ],
    [
      { question: 'Which phase filters out Nodes that cannot run a Pod?', options: ['Scoring', 'Binding', 'Filtering', 'Reserving'], answer: 2, explanation: 'The filtering (predicate) phase eliminates nodes that cannot satisfy the Pod requirements.' },
      { question: 'What happens to Pods when no scheduler is running?', options: ['They run on random nodes', 'They stay Pending', 'They get deleted', 'They are queued'], answer: 1, explanation: 'Without a scheduler, Pods remain in Pending state because no node assignment is made.' },
      { question: 'Which field specifies a custom scheduler for a Pod?', options: ['nodeSelector', 'schedulerName', 'nodeName', 'affinity'], answer: 1, explanation: 'The schedulerName field in a Pod spec specifies which scheduler should handle that Pod.' },
      { question: 'What is the purpose of Pod priority preemption?', options: ['Spread load evenly', 'Evict lower-priority Pods to schedule higher-priority ones', 'Balance resource usage', 'Optimize network latency'], answer: 1, explanation: 'Preemption evicts lower-priority Pods to make resources available for higher-priority Pods.' },
      { question: 'Which scoring strategy spreads Pods across nodes to balance load?', options: ['MostRequestedPriority', 'LeastRequestedPriority', 'BalancedResourceAllocation', 'ImageLocality'], answer: 1, explanation: 'LeastRequestedPriority favors nodes with more available resources, spreading Pods across the cluster.' },
      { question: 'What does PodTopologySpread constraint do?', options: ['Spread Pods across cloud regions', 'Spread Pods evenly across topology domains', 'Co-locate Pods on same node', 'Spread Pods across namespaces'], answer: 1, explanation: 'PodTopologySpreadConstraints ensure Pods are spread evenly across topology domains like zones or nodes.' },
      { question: 'What is the role of the scheduler QueueSort plugin?', options: ['Sort Pods by priority in the scheduling queue', 'Score nodes', 'Filter nodes', 'Bind Pods to nodes'], answer: 0, explanation: 'The QueueSort plugin determines the ordering of Pods in the scheduling queue, typically by priority.' },
      { question: 'Which extension point can approve or reject a scheduling decision?', options: ['Filter', 'Score', 'Permit', 'Bind'], answer: 2, explanation: 'The Permit extension point can approve, deny, or wait (with a timeout) for a scheduling decision.' },
      { question: 'How does the scheduler handle volume topology constraints?', options: ['Ignores them', 'Gets volume from API Server', 'Uses VolumeBinding plugin scheduling', 'Binds volume to any node'], answer: 2, explanation: 'The VolumeBinding plugin considers volume topology constraints and schedules Pods to nodes where the volume is accessible.' },
      { question: 'What happens if a Node has a NoExecute taint?', options: ['Pods cannot be scheduled', 'Existing Pods without toleration are evicted', 'Pods can be scheduled with toleration', 'All of the above'], answer: 3, explanation: 'NoExecute taint prevents new Pods without toleration and evicts existing Pods without toleration.' }
    ],
    [
      { title: 'View Scheduler Logs', useCase: 'Debug scheduling failures', code: 'kubectl logs -n kube-system kube-scheduler-<node>\nkubectl describe pod -n kube-system kube-scheduler-<node>', description: 'View scheduler logs and Pod details to troubleshoot scheduling issues.' },
      { title: 'Check Pending Pods', useCase: 'Identify Pods waiting for scheduling', code: 'kubectl get pods --all-namespaces --field-selector status.phase=Pending\nkubectl describe pod <pending-pod> | findstr Events', description: 'Lists all pending Pods and describes events showing why a Pod is unschedulable.' },
      { title: 'Set Node Affinity', useCase: 'Ensure Pod runs on specific nodes', code: 'apiVersion: v1\nkind: Pod\nmetadata:\n  name: nginx\nspec:\n  affinity:\n    nodeAffinity:\n      requiredDuringSchedulingIgnoredDuringExecution:\n        nodeSelectorTerms:\n        - matchExpressions:\n          - key: disktype\n            operator: In\n            values:\n            - ssd\n  containers:\n  - name: nginx\n    image: nginx', description: 'YAML specifying a required node affinity for ssd-labeled nodes.' },
      { title: 'Create a Custom PriorityClass', useCase: 'Assign priorities for preemption', code: 'apiVersion: scheduling.k8s.io/v1\nkind: PriorityClass\nmetadata:\n  name: high-priority\nvalue: 1000000\nglobalDefault: false\ndescription: "High priority Pods"', description: 'Creates a PriorityClass with value 1,000,000 for critical workloads.' },
      { title: 'Configure Pod Topology Spread', useCase: 'Spread Pods across zones for HA', code: 'apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: web\nspec:\n  replicas: 9\n  template:\n    spec:\n      topologySpreadConstraints:\n      - maxSkew: 1\n        topologyKey: topology.kubernetes.io/zone\n        whenUnsatisfiable: DoNotSchedule\n        labelSelector:\n          matchLabels:\n            app: web', description: 'Deployment with topology spread constraints to distribute Pods evenly across availability zones.' }
    ]
  ),

  // ==============================================================
  // kube-controller-manager
  // ==============================================================
  topic('k8s-controller-manager', 'kube-controller-manager', 'intermediate', 15,
    'The kube-controller-manager is the cluster\'s autopilot. It runs dozens of controller processes that constantly watch the current state of the cluster and make changes to match the desired state. If a Pod dies, the Replication Controller creates a replacement. If a Node goes down, the Node Controller marks it unreachable. Think of controllers as tireless workers who never sleep — they continuously compare "what is" with "what should be" and make corrections.',
    [
      'The Controller Manager bundles multiple controllers into a single binary, each responsible for a specific aspect of cluster management',
      'Key controllers: Node Controller (node health), Replication Controller (Pod count), Endpoint Controller (Service endpoints), Service Account Controller (service account creation)',
      'Controllers use the informer pattern — they watch the API Server for changes and maintain a local cache for efficient reconciliation',
      'The Controller Manager runs with leader election in HA setups — only one instance is active, others stand by',
      'Custom controllers can be built using the controller-runtime library and Operator SDK to extend Kubernetes behavior'
    ],
    [
      { heading: 'Core Controllers and Their Functions', text: 'The Controller Manager includes: Node Controller (monitors node health via heartbeats, manages node status/eviction), Replication Controller (ensures correct number of Pod replicas for ReplicationController resources), ReplicaSet controller (same for ReplicaSet), Deployment controller (manages rollout and rollback of Deployments via ReplicaSets), StatefulSet controller (manages stateful application Pods with stable identities), DaemonSet controller (ensures one Pod per node), Job controller (runs batch Jobs to completion), CronJob controller (schedules Jobs on a cron timetable), EndpointSlice controller (manages EndpointSlice objects for Services), and ServiceAccount controller (creates default service accounts per namespace).' },
      { heading: 'Controller Pattern: Informers, Work Queues, and Reconcilers', text: 'Controllers follow a standard pattern. Informers watch the API Server for resource changes and update a local cache. Events are added to a work queue for processing. The reconciler function reads the desired state from the object spec, reads the current state from the local cache, and takes action to converge them. This pattern is implemented by the controller-runtime library and is the foundation for building custom operators.' },
      { heading: 'Cloud Controller Manager Integration', text: 'Cloud-controller-manager runs controllers specific to cloud providers: Node Controller (manages node labeling, IP addresses, and instance termination), Route Controller (manages cloud network routes for Pod CIDR), and Service Controller (manages cloud Load Balancers for Services of type LoadBalancer). Cloud providers implement their own cloud-controller-manager binaries.' }
    ],
    [
      { question: 'What is the kube-controller-manager?', answer: 'It is a binary that runs Kubernetes control loops — processes that watch cluster state and make changes to move actual state toward desired state.' },
      { question: 'How does the Deployment controller work?', answer: 'The Deployment controller watches Deployment objects and manages ReplicaSets. For a rollout, it creates a new ReplicaSet with the updated Pod template and gradually scales it up while scaling down the old ReplicaSet.' },
      { question: 'What does the Node Controller do?', answer: 'The Node Controller monitors node health via periodic heartbeats (NodeStatus updates from kubelet). If a node stops reporting, it marks conditions Unknown, then after the eviction timeout, evicts Pods from the unreachable node.' },
      { question: 'What is the difference between ReplicationController and ReplicaSet?', answer: 'ReplicaSet is the successor to ReplicationController with richer label selector support (matchLabels and matchExpressions). ReplicationController is deprecated.' },
      { question: 'What controllers handle batch workloads?', answer: 'The Job controller runs Pods to completion and ensures success. The CronJob controller creates Job objects on a schedule defined by a cron expression.' },
      { question: 'How does the Service controller work?', answer: 'For Services of type LoadBalancer, the Service controller (in cloud-controller-manager) provisions a cloud load balancer and updates the Service with the external IP. For type ClusterIP or NodePort, the EndpointSlice controller manages endpoint discovery.' },
      { question: 'What happens when a controller crashes?', answer: 'If the controller-manager Pod crashes, the leader election system detects the heartbeat timeout and another instance takes over. Reconciliation loops resume within seconds.' },
      { question: 'Can you write custom controllers?', answer: 'Yes. Custom controllers (operators) can be written using the controller-runtime library and client-go. They watch custom resources (CRDs) and reconcile desired state. Popular examples include Prometheus Operator, Cert-Manager, and Ingress Controllers.' },
      { question: 'How does the DaemonSet controller work?', answer: 'The DaemonSet controller watches for Node additions and ensures a Pod is running on every node (or a subset via nodeSelector). When a node is removed, the DaemonSet Pod is also removed.' },
      { question: 'What is the end-to-end reconcile loop?', answer: '1) Informer detects a resource change (e.g., Deployment updated). 2) Event is added to work queue. 3) Reconciler reads desired state (3 replicas of nginx:1.20). 4) Checks current state (2 replicas running nginx:1.19). 5) Creates a new ReplicaSet and initiates a rolling update. 6) Updates resource status.' }
    ],
    [
      { question: 'Which controller ensures the correct number of Pod replicas?', options: ['Deployment controller', 'ReplicaSet controller', 'Node controller', 'Job controller'], answer: 1, explanation: 'The ReplicaSet controller ensures the specified number of Pod replicas are running at all times.' },
      { question: 'What happens when a Node stops sending heartbeats?', options: ['Nothing', 'Node Controller marks it unhealthy and evicts Pods', 'kube-scheduler restarts the node', 'API Server deletes the node'], answer: 1, explanation: 'The Node Controller marks the node ConditionUnknown and eventually evicts Pods after the eviction timeout.' },
      { question: 'Which controller manages rollout of new versions?', options: ['ReplicaSet controller', 'Deployment controller', 'StatefulSet controller', 'DaemonSet controller'], answer: 1, explanation: 'The Deployment controller manages rolling updates and rollbacks by coordinating ReplicaSets.' },
      { question: 'How does leader election work in the Controller Manager?', options: ['etcd lease', 'Kubernetes Endpoints/Lease objects', 'DNS voting', 'Manual failover'], answer: 1, explanation: 'Controller Manager uses Lease or Endpoints objects in the kube-system namespace for leader election.' },
      { question: 'Which controller creates default service accounts?', options: ['Token controller', 'ServiceAccount controller', 'RBAC controller', 'Namespace controller'], answer: 1, explanation: 'The ServiceAccount controller ensures each namespace has a default ServiceAccount.' },
      { question: 'What is the cloud-controller-manager responsible for?', options: ['Pod scheduling', 'Cloud provider-specific controllers', 'Container runtime', 'Network policy'], answer: 1, explanation: 'Cloud controller manager handles cloud-specific resources like load balancers, routes, and node management.' },
      { question: 'Which controller ensures one Pod per node for logging agents?', options: ['Deployment', 'StatefulSet', 'DaemonSet', 'Job'], answer: 2, explanation: 'DaemonSet ensures one Pod per node, ideal for logging agents, monitoring, and CNI plugins.' },
      { question: 'What is an operator in Kubernetes?', options: ['CLI tool', 'Custom controller managing complex applications', 'API Server plugin', 'Scheduling plugin'], answer: 1, explanation: 'An operator is a custom controller that manages complex applications using Custom Resource Definitions.' },
      { question: 'Which binary contains most built-in controllers?', options: ['kube-apiserver', 'kube-controller-manager', 'kube-scheduler', 'kubelet'], answer: 1, explanation: 'kube-controller-manager contains most built-in controllers including Node, ReplicaSet, Deployment, and others.' },
      { question: 'What does the CronJob controller do?', options: ['Runs Jobs continuously', 'Runs Jobs on a schedule', 'Runs one-time Jobs', 'Monitors Job status'], answer: 1, explanation: 'CronJob creates Job objects on a cron-defined schedule, enabling periodic batch processing.' }
    ],
    [
      { title: 'List Running Controllers', useCase: 'See which controllers are active', code: 'kubectl get pods -n kube-system -l component=kube-controller-manager\nkubectl logs -n kube-system kube-controller-manager-<node> | head -50', description: 'Lists Controller Manager Pods and views recent controller activity logs.' },
      { title: 'Check Controller Manager Metrics', useCase: 'Monitor controller performance', code: 'kubectl get --raw /metrics -n kube-system | findstr controller | findstr work_queue', description: 'Scrapes controller work queue metrics showing queue depth and processing latency.' },
      { title: 'Scale a Deployment (triggers controller)', useCase: 'Watch Deployment controller respond', code: 'kubectl scale deployment nginx --replicas=5\nkubectl get pods -l app=nginx --watch', description: 'Scales a Deployment and watches the controller create new ReplicaSet Pods.' },
      { title: 'Create a Custom Resource and Controller', useCase: 'Extend Kubernetes with custom logic', code: 'apiVersion: apiextensions.k8s.io/v1\nkind: CustomResourceDefinition\nmetadata:\n  name: myapps.example.com\nspec:\n  group: example.com\n  names:\n    kind: MyApp\n    plural: myapps\n  scope: Namespaced\n  versions:\n  - name: v1\n    served: true\n    storage: true\n    schema:\n      openAPIV3Schema:\n        type: object\n        properties:\n          spec:\n            type: object\n            properties:\n              replicas:\n                type: integer', description: 'Defines a custom resource for a custom controller to manage.' },
      { title: 'Watch Controller Reconciliation', useCase: 'Observe the reconcile loop in action', code: 'kubectl delete pod <pod-name>\nkubectl get pods -w', description: 'Delete a Pod and watch the ReplicaSet controller immediately create a replacement.' }
    ]
  ),

  // ==============================================================
  // etcd
  // ==============================================================
  topic('k8s-etcd', 'etcd', 'intermediate', 15,
    'etcd is the cluster\'s source of truth — it stores everything about your cluster in a reliable, distributed key-value store. Every Kubernetes object (Pods, ConfigMaps, Secrets, Deployments, etc.) is serialized and stored in etcd. It uses the Raft consensus algorithm to ensure data consistency across multiple replicas. If you think of the cluster as a living organism, etcd is its memory — without it, the cluster cannot remember anything or make informed decisions.',
    [
      'etcd is a distributed, consistent key-value store that serves as Kubernetes\' single source of truth for all cluster state',
      'It uses the Raft consensus algorithm to maintain consistency across a cluster of etcd nodes, tolerating minority failures',
      'Only the API Server communicates directly with etcd — all other components interact through the API Server',
      'etcd supports watch semantics, enabling event-driven architecture where clients receive real-time change notifications',
      'Regular etcd backups are critical for disaster recovery — a corrupted etcd can render the entire cluster inoperable'
    ],
    [
      { heading: 'etcd Architecture and Raft Consensus', text: 'etcd organizes a cluster of nodes where one is the leader and the rest are followers. The leader handles all client requests (writes go through the leader). Raft ensures consensus: the leader logs each write and replicates it to a majority (quorum) of followers before committing. For a 3-node cluster, write requires 2/3 nodes to acknowledge. The leader is elected automatically; if it fails, followers hold an election and a new leader takes over within milliseconds.' },
      { heading: 'etcd Data Model and Storage', text: 'etcd stores data as a hierarchical key-value store (like a filesystem). Kubernetes stores objects as JSON-serialized values under keys like /registry/pods/default/nginx-pod, /registry/deployments/default/web, /registry/services/endpoints/default/kubernetes. Each key has a revision (monotonically increasing integer) used for watch ordering and optimistic concurrency. etcd supports multi-version concurrency control (MVCC) — older revisions are retained for a configurable window to support watches and history.' },
      { heading: 'etcd Performance, Backup, and Recovery', text: 'etcd performance depends on disk speed (SSD required) and network latency between cluster members. Key operations are limited to 1.5 MB per request (default). Production etcd should run on dedicated SSDs. Backup: etcdctl snapshot save captures a consistent snapshot. For recovery: stop API Server, restore snapshot to a new data directory, start etcd with restored data, verify cluster health, start API Server. Regular backups (every 2 hours, retain 10+ days) are essential.' }
    ],
    [
      { question: 'What is etcd in Kubernetes?', answer: 'etcd is a distributed, consistent key-value store that stores all Kubernetes cluster data. It is the single source of truth for cluster state.' },
      { question: 'Why does Kubernetes use etcd instead of a traditional database?', answer: 'etcd provides strong consistency (linearizable reads), watch support (real-time notifications on changes), high availability (Raft consensus), and is designed for the operational patterns Kubernetes needs — storing configuration, not large data.' },
      { question: 'How does etcd handle failures?', answer: 'etcd uses Raft consensus. A cluster of 3 etcd nodes can tolerate 1 failure. With 5 nodes, 2 failures are tolerated. The cluster continues operating as long as a majority is available.' },
      { question: 'What is a quorum in etcd?', answer: 'Quorum is the minimum number of nodes that must agree on a write for it to be committed. For N nodes, quorum = floor(N/2) + 1. For 3 nodes, quorum is 2. For 5 nodes, quorum is 3.' },
      { question: 'How do you backup etcd?', answer: 'Use etcdctl snapshot save to create a snapshot file. For automated backups, tools like Velero or etcdadm can be used. Backups should be stored off-cluster and tested periodically.' },
      { question: 'What happens if etcd data is corrupted?', answer: 'The cluster becomes unstable or inoperable. The API Server may crash or return errors. Recovery requires restoring from a backup snapshot and restarting etcd.' },
      { question: 'How does the API Server interact with etcd?', answer: 'The API Server sends watch, get, create, update, and delete requests to etcd via its gRPC API. Objects are serialized to JSON (or Protobuf) and stored with their resource version as the etcd revision.' },
      { question: 'What is the recommended disk for etcd?', answer: 'NVMe SSDs are recommended. etcd is sensitive to write latency — a slow disk can cause leader election failures and cluster instability. The fsync latency should be under 10ms (ideally under 2ms).' },
      { question: 'How large should an etcd cluster be?', answer: '3 nodes for most production clusters, 5 nodes for large-scale deployments. Etcd clusters should not exceed 7 nodes, as the Raft overhead increases with more nodes.' },
      { question: 'What is the maximum request size in etcd?', answer: 'The default maximum request size is 1.5 MB. This limits the size of Kubernetes objects — any object larger than this cannot be stored in etcd.' }
    ],
    [
      { question: 'Which consensus algorithm does etcd use?', options: ['Paxos', 'Raft', 'Zab', 'Gossip'], answer: 1, explanation: 'etcd uses the Raft consensus algorithm for leader election and log replication.' },
      { question: 'What is quorum for a 5-node etcd cluster?', options: ['2', '3', '4', '5'], answer: 1, explanation: 'Quorum = floor(N/2)+1 = floor(5/2)+1 = 2+1 = 3 nodes for a 5-node cluster.' },
      { question: 'Which component communicates directly with etcd?', options: ['kube-scheduler', 'kube-apiserver', 'kube-controller-manager', 'kubelet'], answer: 1, explanation: 'Only the API Server directly reads and writes to etcd. All other components go through the API Server.' },
      { question: 'What storage type is recommended for etcd?', options: ['HDD', 'SSD or NVMe', 'Network storage', 'RAM disk'], answer: 1, explanation: 'etcd requires low-latency storage. SSDs (ideally NVMe) are strongly recommended for reliable performance.' },
      { question: 'What is the default maximum request size in etcd?', options: ['512 KB', '1 MB', '1.5 MB', '10 MB'], answer: 2, explanation: 'The default maximum request size is 1.5 MB, limiting the maximum Kubernetes object size.' },
      { question: 'How many node failures can a 3-node etcd cluster tolerate?', options: ['0', '1', '2', '3'], answer: 1, explanation: 'A 3-node cluster needs a majority (2 nodes) for quorum, so it tolerates 1 node failure.' },
      { question: 'What tool creates etcd snapshots?', options: ['kubectl', 'etcdctl', 'kubeadm', 'helm'], answer: 1, explanation: 'etcdctl snapshot save creates a consistent snapshot of etcd data.' },
      { question: 'What happens when etcd leader fails?', options: ['Cluster stops', 'New leader is elected automatically', 'API Server takes over', 'Data is lost'], answer: 1, explanation: 'etcd automatically holds a Raft election and a new leader is elected, typically within milliseconds.' },
      { question: 'What MVCC property does etcd support?', options: ['Versioned keys', 'Multi-version concurrency control', 'Parallel writes', 'Snapshot isolation'], answer: 1, explanation: 'etcd supports MVCC by retaining multiple revisions of keys for watch and history support.' },
      { question: 'Where does Kubernetes store Pod data in etcd?', options: ['/pods/default/', '/registry/pods/', '/kubernetes/pods/', '/data/pods/'], answer: 1, explanation: 'Kubernetes stores all registry data under /registry/, with Pods under /registry/pods/<namespace>/<name>.' }
    ],
    [
      { title: 'Check etcd Cluster Health', useCase: 'Verify etcd quorum and member health', code: 'kubectl exec -n kube-system etcd-<node> -- etcdctl endpoint health --cluster --cacert /etc/kubernetes/pki/etcd/ca.crt --cert /etc/kubernetes/pki/etcd/healthcheck-client.crt --key /etc/kubernetes/pki/etcd/healthcheck-client.key', description: 'Checks health of all etcd cluster members.' },
      { title: 'List etcd Members', useCase: 'View etcd cluster membership', code: 'kubectl exec -n kube-system etcd-<node> -- etcdctl member list --cacert /etc/kubernetes/pki/etcd/ca.crt --cert /etc/kubernetes/pki/etcd/healthcheck-client.crt --key /etc/kubernetes/pki/etcd/healthcheck-client.key', description: 'Lists all etcd cluster members with their roles and endpoints.' },
      { title: 'Backup etcd Data', useCase: 'Create a snapshot for disaster recovery', code: 'kubectl exec -n kube-system etcd-<node> -- etcdctl snapshot save /var/lib/etcd/snapshot.db --cacert /etc/kubernetes/pki/etcd/ca.crt --cert /etc/kubernetes/pki/etcd/healthcheck-client.crt --key /etc/kubernetes/pki/etcd/healthcheck-client.key\nkubectl cp kube-system/etcd-<node>:/var/lib/etcd/snapshot.db ./snapshot.db', description: 'Creates an etcd snapshot and copies it locally.' },
      { title: 'Restore etcd from Snapshot', useCase: 'Recover cluster after data corruption', code: 'etcdctl snapshot restore snapshot.db --data-dir /var/lib/etcd-restore --cacert /etc/kubernetes/pki/etcd/ca.crt --cert /etc/kubernetes/pki/etcd/healthcheck-client.crt --key /etc/kubernetes/pki/etcd/healthcheck-client.key\nmv /var/lib/etcd /var/lib/etcd-backup\nmv /var/lib/etcd-restore /var/lib/etcd', description: 'Restores etcd data from a snapshot file to a new data directory.' },
      { title: 'Check etcd Metrics', useCase: 'Monitor etcd performance and latency', code: 'kubectl get --raw /api/v1/namespaces/kube-system/pods/etcd-<node>:2379/proxy/metrics | findstr etcd_disk_wal_fsync_duration_seconds', description: 'Views etcd fsync duration metrics to identify disk I/O issues.' }
    ]
  ),

  // ==============================================================
  // kubelet
  // ==============================================================
  topic('k8s-kubelet', 'kubelet', 'intermediate', 10,
    'The kubelet is the node agent — it is the primary Kubernetes component running on every Worker Node. Think of it as the node\'s personal assistant that follows orders from the Control Plane. When the scheduler assigns a Pod to a node, kubelet is responsible for pulling the container image, starting the containers, running health checks, reporting status, and cleaning up when the Pod is deleted. Without kubelet, a node is just a server — kubelet makes it a Kubernetes node.',
    [
      'Kubelet is the primary node agent that runs on every node and manages Pod lifecycle based on PodSpecs from the API Server',
      'It registers the node with the cluster, manages container lifecycle via CRI, runs probes (liveness, readiness, startup), and reports node/Pod status',
      'Kubelet integrates with the container runtime through the Container Runtime Interface (CRI), supporting containerd, CRI-O, and others',
      'It enforces resource limits via cgroups, manages volume mounts, handles Pod eviction under resource pressure, and collects node-level metrics',
      'Kubelet can also run static Pods from local manifest files, typically used for Control Plane components in kubeadm clusters'
    ],
    [
      { heading: 'Pod Lifecycle Management', text: 'When the API Server assigns a Pod to a node, kubelet creates the Pod sandbox (pause container for namespaces), pulls container images (via CRI), creates and starts containers, sets up networking (via CNI), mounts volumes, runs init containers sequentially, starts sidecar and main containers, and runs probes continuously. When a Pod needs to terminate, kubelet runs preStop hooks, sends SIGTERM, waits for grace period, then sends SIGKILL.' },
      { heading: 'Node Status and Heartbeat', text: 'Kubelet periodically sends NodeStatus updates to the API Server containing node conditions (Ready, DiskPressure, MemoryPressure, PIDPressure), allocatable resources, running Pods list, and node addresses. The update frequency is controlled by --node-status-update-frequency (default 10s). Heartbeats are lightweight Lease objects that renew more frequently than full NodeStatus updates, reducing API Server load.' },
      { heading: 'Resource Management and Pod Eviction', text: 'Kubelet manages node resources by reserving system-reserved and kube-reserved resources, then allocating the rest to Pods. It monitors disk pressure (eviction thresholds like 10% free on rootfs), memory pressure (eviction threshold), and PID pressure. When thresholds are exceeded, kubelet evicts Pods in priority order: BestEffort first, then Burstable, then Guaranteed. Key thresholds include memory.available, nodefs.available, nodefs.inodesFree, and imagefs.available.' }
    ],
    [
      { question: 'What is the kubelet?', answer: 'The kubelet is the primary node agent that runs on every Kubernetes node. It manages Pods, containers, and ensures they are running and healthy.' },
      { question: 'How does kubelet communicate with the API Server?', answer: 'Kubelet communicates with the API Server using TLS client certificates. It watches for Pod assignments, reports node and Pod status, and responds to requests like exec, logs, and port-forward.' },
      { question: 'What is the Container Runtime Interface (CRI)?', answer: 'CRI is a plugin interface that allows kubelet to use different container runtimes without recompilation. It defines gRPC APIs for kubelet to manage containers and images.' },
      { question: 'What probes does kubelet support?', answer: 'Kubelet runs three types of probes: Liveness (restart container if fails), Readiness (remove from Service if fails), and Startup (delays liveness/readiness for slow-starting containers).' },
      { question: 'What happens when kubelet crashes?', answer: 'Existing containers continue running, but no new Pods are scheduled on the node, health checks stop, and the node eventually becomes NotReady. kubelet must be restarted (typically managed by systemd as a service).' },
      { question: 'How does kubelet pull container images?', answer: 'Kubelet delegates image pulling to the container runtime via CRI. It supports imagePullPolicy: Always (always pull), IfNotPresent (pull only if not cached), and Never (never pull).' },
      { question: 'What is a static Pod?', answer: 'A static Pod is a Pod managed directly by kubelet from a YAML/JSON manifest file in the kubelet manifest directory (default /etc/kubernetes/manifests/). kubelet watches this directory and creates/deletes Pods accordingly. API Server can see static Pods but cannot manage them.' },
      { question: 'What is the --max-pods flag?', answer: 'It sets the maximum number of Pods this kubelet will run. Default is 110. Cloud providers may set lower limits based on network interface limits per instance.' },
      { question: 'How does kubelet handle volume mounting?', answer: 'Kubelet mounts volumes defined in the PodSpec into containers. It supports many volume types: configMap, secret, emptyDir, hostPath, persistentVolumeClaim, projected, and many more. It uses CSI for external volume drivers.' },
      { question: 'What is the Pod lifecycle hook?', answer: 'Containers can define postStart (runs after container starts) and preStop (runs before container terminates) hooks. These can be exec (run command inside container) or HTTP (make HTTP request) actions.' }
    ],
    [
      { question: 'Which component manages the container lifecycle on a node?', options: ['kube-apiserver', 'kube-scheduler', 'kubelet', 'kube-proxy'], answer: 2, explanation: 'Kubelet is the node agent responsible for managing Pods and containers on each node.' },
      { question: 'What interface does kubelet use to communicate with container runtimes?', options: ['OCI', 'CRI', 'CNI', 'CSI'], answer: 1, explanation: 'CRI (Container Runtime Interface) defines the API between kubelet and container runtimes.' },
      { question: 'What happens if a liveness probe fails?', options: ['Pod is deleted', 'Container is restarted', 'Pod is rescheduled', 'Node is marked unhealthy'], answer: 1, explanation: 'A failing liveness probe causes kubelet to restart the container. Exponential backoff is applied.' },
      { question: 'Where does kubelet look for static Pod manifests?', options: ['/etc/kubernetes/', '/etc/kubernetes/manifests/', '/var/lib/kubelet/', '/etc/default/'], answer: 1, explanation: 'The default static Pod manifest directory is /etc/kubernetes/manifests/.' },
      { question: 'What is the default --max-pods value for kubelet?', options: ['50', '110', '250', 'Unlimited'], answer: 1, explanation: 'The default maximum number of Pods per node is 110.' },
      { question: 'Which QoS class gets evicted first under memory pressure?', options: ['Guaranteed', 'Burstable', 'BestEffort', 'System'], answer: 2, explanation: 'BestEffort Pods (no resource requests or limits) are evicted first under resource pressure.' },
      { question: 'What protocol does kubelet use to serve metrics?', options: ['HTTP', 'HTTPS', 'gRPC', 'Unix socket'], answer: 1, explanation: 'Kubelet serves metrics and the read-only API over HTTPS on port 10250 by default.' },
      { question: 'What is the role of the pause container in a Pod?', options: ['Run main application', 'Hold network namespaces', 'Monitor health', 'Proxy traffic'], answer: 1, explanation: 'The pause container holds the network and PID namespace for the Pod, allowing other containers to share them.' },
      { question: 'How does kubelet enforce CPU limits?', options: ['cgroups', 'nice values', 'cpulimit', 'task sets'], answer: 0, explanation: 'Kubelet uses cgroups (control groups) to enforce CPU and memory limits for containers.' },
      { question: 'What is the NodeStatus update frequency default?', options: ['5s', '10s', '30s', '60s'], answer: 1, explanation: 'The default --node-status-update-frequency is 10 seconds.' }
    ],
    [
      { title: 'Check Kubelet Logs', useCase: 'Troubleshoot node-level Pod issues', code: 'Get-Content /var/log/kubelet.log -Tail 100\nkubectl logs --tail=50 kubelet-<node> -n kube-system', description: 'View recent kubelet logs for debugging Pod scheduling and runtime issues.' },
      { title: 'View Kubelet Configuration', useCase: 'Check kubelet flags and settings', code: 'kubectl describe node <node-name> | findstr -A10 "Conditions"\nkubectl get --raw /api/v1/nodes/<node-name>/proxy/configz', description: 'Shows the running kubelet configuration and node conditions.' },
      { title: 'Check Node Resource Pressure', useCase: 'Identify resource pressure triggers', code: 'kubectl describe node <node-name> | findstr -A5 "Conditions"\nkubectl top node <node-name>', description: 'Checks node conditions for DiskPressure, MemoryPressure, or PIDPressure.' },
      { title: 'Restart Kubelet', useCase: 'Apply kubelet config changes or recover from crash', code: 'sudo systemctl status kubelet\nsudo systemctl restart kubelet', description: 'Restarts the kubelet service (requires SSH access to the node).' },
      { title: 'Port Forward via Kubelet API', useCase: 'Debug Pod without kubectl port-forward', code: 'kubectl get --raw /api/v1/namespaces/default/pods/<pod>/proxy/port/8080\nkubectl exec <pod> -- env', description: 'Uses kubelet proxy to access Pod endpoints directly.' }
    ]
  ),

  // ==============================================================
  // kube-proxy
  // ==============================================================
  topic('k8s-kube-proxy', 'kube-proxy', 'intermediate', 10,
    'kube-proxy is the network traffic director on each node. Its job is to make Services work — it translates virtual Service IP addresses into actual Pod IP addresses. When you create a Service, kube-proxy on every node programs the local firewall rules (iptables or IPVS) so that traffic to the Service\'s ClusterIP is load-balanced to healthy Pods. Think of it as a postal sorting facility — it reads the destination address (Service IP) and forwards the package to the right mailbox (Pod IP).',
    [
      'kube-proxy is a network proxy that runs on every node, implementing Service-to-Pod traffic routing via iptables, IPVS, or userspace mode',
      'It watches the API Server for Service and EndpointSlice changes and updates local networking rules to reflect the current state',
      'iptables mode uses Linux netfilter rules for each Service — simple but can cause performance issues in large clusters (thousands of Services)',
      'IPVS mode uses the Linux kernel IP Virtual Server module for better scalability and performance with large numbers of Services',
      'kube-proxy also handles ClusterIP, NodePort, and LoadBalancer Service types, and manages external traffic policies (local vs cluster)'
    ],
    [
      { heading: 'kube-proxy Modes: iptables vs IPVS', text: 'iptables mode (default) creates an iptables rule for each Service and each backend Pod. For a cluster with 10,000 Services, iptables rules can take hours to update due to O(n) complexity. IPVS mode uses the kernel IPVS table, which has O(1) complexity for lookups and uses hash tables for efficient matching. IPVS supports more scheduling algorithms (rr, wrr, lc, dh, sh, sed, nq) compared to iptables\' random selection. Most production clusters should use IPVS mode for better scalability.' },
      { heading: 'Service Traffic Flow and External Traffic Policy', text: 'When a Service receives traffic, kube-proxy must decide which Pod receives it. With externalTrafficPolicy: Cluster (default), traffic can go to any Pod on any node, requiring SNAT (source network address translation) if the Pod is on a different node — preserving the client IP is problematic. With externalTrafficPolicy: Local, traffic is only forwarded to Pods on the same node, preserving the original client IP but potentially causing uneven load distribution.' },
      { heading: 'kube-proxy and Network Policy Interaction', text: 'kube-proxy handles Service traffic routing, but it does NOT implement NetworkPolicy. Network policies are implemented by the CNI plugin (Calico, Cilium, Weave). kube-proxy operates at layer 4 (transport) — TCP, UDP, SCTP — and does not understand layer 7 protocols. For layer 7 routing and policy, an Ingress controller or service mesh (Istio, Linkerd) is needed.' }
    ],
    [
      { question: 'What is kube-proxy responsible for?', answer: 'kube-proxy maintains network rules on each node to implement the Kubernetes Service abstraction — routing traffic from Service ClusterIPs to backend Pod IPs.' },
      { question: 'What are the different kube-proxy modes?', answer: 'Three modes: userspace (deprecated, old user-mode proxy), iptables (default, uses netfilter rules), and IPVS (recommended for large clusters, uses kernel IPVS tables).' },
      { question: 'Does kube-proxy support load balancing?', answer: 'Yes. kube-proxy load-balances traffic across Pod backends for a Service. In iptables mode, selection is random. In IPVS mode, multiple scheduling algorithms are available (round-robin, least connections, etc.).' },
      { question: 'How does kube-proxy know which Pods back a Service?', answer: 'kube-proxy watches EndpointSlice (or Endpoints) objects from the API Server. These objects list the Pod IPs and ports that match the Service selector. When Pods change, EndpointSlices are updated and kube-proxy recalculates rules.' },
      { question: 'What is the difference between ClusterIP and NodePort?', answer: 'ClusterIP is a virtual IP reachable only within the cluster. NodePort exposes the Service on a static port on every node\'s IP address, making it accessible from outside the cluster.' },
      { question: 'How does IPVS mode improve over iptables?', answer: 'IPVS uses hash tables with O(1) lookup complexity, supports more scheduling algorithms, and handles large numbers of Services (10,000+) without performance degradation.' },
      { question: 'What is the externalTrafficPolicy?', answer: 'Cluster: traffic can be forwarded to any Pod in the cluster, may SNAT the source IP. Local: traffic only forwarded to Pods on the same node, preserving source IP but potentially uneven load distribution.' },
      { question: 'Can kube-proxy be used without Services?', answer: 'kube-proxy\'s sole purpose is Service traffic routing. Without Services, kube-proxy has no rules to configure. Pod-to-Pod direct communication is handled by the CNI plugin, not kube-proxy.' },
      { question: 'What ports does kube-proxy use?', answer: 'kube-proxy uses port 10249 for metrics (health check) and port 10256 for the readiness probe. It does not listen on application ports — it programs iptables/IPVS rules, not a listening socket.' },
      { question: 'How to check if kube-proxy is working?', answer: 'Check kube-proxy Pod logs, verify iptables rules exist for Services (iptables -L -t nat), or test Service connectivity from a Pod using the Service DNS name.' }
    ],
    [
      { question: 'What is the default kube-proxy mode?', options: ['userspace', 'iptables', 'IPVS', 'nftables'], answer: 1, explanation: 'The default proxy mode is iptables, using Linux netfilter rules for Service traffic routing.' },
      { question: 'Which mode is recommended for large clusters with many Services?', options: ['userspace', 'iptables', 'IPVS', 'nftables'], answer: 2, explanation: 'IPVS mode is recommended for large clusters because it scales better using kernel hash tables with O(1) lookup.' },
      { question: 'What does externalTrafficPolicy: Local preserve?', options: ['Bandwidth', 'Source IP address', 'Service IP', 'Pod priority'], answer: 1, explanation: 'Local policy preserves the original client IP address because traffic is not SNAT-ed when forwarded to a local Pod.' },
      { question: 'Which API resource does kube-proxy watch for Pod backends?', options: ['Pods', 'Endpoints or EndpointSlice', 'Services', 'ConfigMaps'], answer: 1, explanation: 'kube-proxy watches EndpointSlice (or Endpoints) to discover which Pod IPs belong to each Service.' },
      { question: 'What happens if kube-proxy is not running?', options: ['Pods cannot communicate', 'Services stop working for cluster-internal traffic', 'The node is deleted', 'Everything works normally'], answer: 1, explanation: 'Without kube-proxy, Service ClusterIPs and NodePorts stop working because the iptables/IPVS rules are not maintained.' },
      { question: 'Which network layer does kube-proxy operate at?', options: ['Layer 2 (Data Link)', 'Layer 3 (Network)', 'Layer 4 (Transport)', 'Layer 7 (Application)'], answer: 2, explanation: 'kube-proxy handles TCP, UDP, and SCTP traffic — operating at Layer 4 of the OSI model.' },
      { question: 'How does iptables mode handle conntrack?', options: ['Ignores conntrack', 'Uses conntrack for NAT', 'Disables conntrack', 'Stateless routing'], answer: 1, explanation: 'iptables mode uses connection tracking (conntrack) for NAT to route traffic between Services and Pods.' },
      { question: 'What IPVS scheduling algorithm distributes connections evenly?', options: ['lc (least connection)', 'rr (round-robin)', 'sh (source hashing)', 'sed (shortest expected delay)'], answer: 1, explanation: 'Round-robin (rr) distributes new connections sequentially across available backend Pods.' },
      { question: 'What is the kube-proxy metrics port?', options: ['6443', '10249', '10250', '10256'], answer: 1, explanation: 'kube-proxy exposes metrics on port 10249 and readiness check on port 10256.' },
      { question: 'Which component implements NetworkPolicy?', options: ['kube-proxy', 'CNI plugin', 'kubelet', 'API Server'], answer: 1, explanation: 'NetworkPolicy is implemented by the CNI plugin (Calico, Cilium, etc.), not by kube-proxy.' }
    ],
    [
      { title: 'Check kube-proxy Mode', useCase: 'Determine which proxy mode is active', code: 'kubectl logs -n kube-system kube-proxy-<node> | findstr "Using"', description: 'View kube-proxy startup logs to see which proxy mode is configured.' },
      { title: 'View iptables Rules for a Service', useCase: 'Inspect Service traffic routing rules', code: 'kubectl get svc kubernetes\nkubectl exec -n kube-system kube-proxy-<node> -- iptables -t nat -L -n | findstr <CLUSTER-IP>', description: 'Inspects iptables NAT rules for the kubernetes Service.' },
      { title: 'Test Service Connectivity from a Pod', useCase: 'Verify Service DNS and routing works', code: 'kubectl run test-pod --image=busybox --rm -it --restart=Never -- wget -O- http://<service-name>.<namespace>.svc.cluster.local:80', description: 'Creates a temporary Pod to test Service connectivity via DNS name.' },
      { title: 'Check kube-proxy Configuration', useCase: 'View current kube-proxy configuration', code: 'kubectl get configmap -n kube-system kube-proxy -o yaml\nkubectl describe daemonset -n kube-system kube-proxy', description: 'Shows the kube-proxy ConfigMap and DaemonSet configuration.' },
      { title: 'Verify EndpointSlices for a Service', useCase: 'Confirm Pods are discovered as Service backends', code: 'kubectl get endpointslices -l kubernetes.io/service-name=<service-name>\nkubectl describe service <service-name>', description: 'Lists EndpointSlices and their ready/unready addresses for a Service.' }
    ]
  ),

  // ==============================================================
  // Container Runtime
  // ==============================================================
  topic('k8s-container-runtime', 'Container Runtime', 'intermediate', 10,
    'The container runtime is the software that actually runs your containers. While Kubernetes manages the orchestra, the container runtime plays the instruments. It pulls container images from registries, creates container filesystems from layered images, runs containers using cgroups and namespaces, and manages the container lifecycle. Kubernetes uses the Container Runtime Interface (CRI) to support multiple runtimes — like containerd and CRI-O — without needing to modify Kubernetes itself.',
    [
      'The container runtime is the software responsible for pulling images, creating containers, and managing their lifecycle on each node',
      'Kubernetes uses CRI (Container Runtime Interface) as an abstraction layer, allowing any OCI-compliant runtime to be used interchangeably',
      'containerd is the most common CRI runtime (default since Kubernetes 1.24), with CRI-O as a lightweight alternative focused on Kubernetes',
      'The runtime handles image management (pulling, caching, garbage collection), container isolation (namespaces, cgroups), and filesystem (overlayfs)',
      'Runtimes can integrate with gVisor or Kata Containers for enhanced security isolation between containers on the same host'
    ],
    [
      { heading: 'Container Runtime Interface (CRI)', text: 'CRI defines a gRPC API with two services: RuntimeService (managing Pod sandboxes and containers) and ImageService (managing images). Kubelet acts as the client, the runtime (containerd, CRI-O) acts as the server. This abstraction means you can swap runtimes by changing the kubelet configuration without recompiling Kubernetes. The CRI shim (e.g., containerd\'s CRI plugin) translates CRI calls into runtime-specific operations.' },
      { heading: 'containerd Architecture and Features', text: 'containerd is a graduated CNCF project that manages the complete container lifecycle. It handles image transfer (pull/push from registries), image storage (content-addressable storage, deduplication), container execution (runC), and network interfaces. It exposes both a gRPC API (CRI plugin for Kubernetes) and a CLI (ctr). containerd\'s architecture has core daemon (containerd), namespace management, snapshot drivers (overlayfs, devmapper), and metadata storage (boltdb).' },
      { heading: 'Advanced Runtime Security with gVisor and Kata', text: 'Standard runc containers share the host kernel, which can be a security concern for multi-tenant environments. gVisor provides an application kernel (Sentry) that intercepts syscalls between the container and host kernel, adding a security layer. Kata Containers run each container in a lightweight VM with its own kernel. Both provide stronger isolation at the cost of higher resource overhead and reduced compatibility.' }
    ],
    [
      { question: 'What is the container runtime in Kubernetes?', answer: 'The container runtime is the software that manages container execution — pulling images, creating containers, managing filesystem layers, and running containers using kernel features like cgroups and namespaces.' },
      { question: 'What is CRI?', answer: 'CRI (Container Runtime Interface) is a plugin interface that allows kubelet to use different container runtimes without recompilation. It defines a gRPC API for container and image management.' },
      { question: 'What is the default container runtime in Kubernetes 1.24+?', answer: 'containerd is the default and recommended runtime since Kubernetes 1.24, when Docker support was removed from kubelet.' },
      { question: 'What is the difference between containerd and Docker?', answer: 'Docker is a complete container platform with CLI, API, and management tools. containerd is a focused container runtime that handles the core container lifecycle. containerd is lighter, has less overhead, and is designed specifically for integration with orchestrators like Kubernetes.' },
      { question: 'What is CRI-O?', answer: 'CRI-O is a lightweight CRI implementation specifically built for Kubernetes. It uses runc (or any OCI runtime) and supports container image formats from any container registry. It is an alternative to containerd.' },
      { question: 'How does the runtime handle container isolation?', answer: 'Containers use Linux namespaces (PID, Network, Mount, UTS, IPC, User, Cgroup) for process isolation and cgroups for resource limits (CPU, memory, I/O). The combined kernel features prevent containers from seeing or interfering with each other.' },
      { question: 'What is an OCI runtime specification?', answer: 'The Open Container Initiative (OCI) defines standards for container runtimes and images. runC is the reference implementation. runtimes that comply with OCI can be used with any CRI-compliant system.' },
      { question: 'What is gVisor?', answer: 'gVisor is a container runtime sandbox that provides an additional security layer by intercepting application syscalls and handling them in a user-space kernel (Sentry), preventing direct host kernel access from containers.' },
      { question: 'How does image pulling work in the runtime?', answer: 'The runtime pulls images layer by layer from the registry. It uses content-addressable storage and caches layers locally. Layers are unpacked into the snapshotter (e.g., overlayfs). The runtime supports imagePullPolicy for caching behavior.' },
      { question: 'What is the role of runC?', answer: 'runC is the OCI reference implementation of the container runtime spec. It creates and runs containers based on OCI bundles. Both containerd and CRI-O use runC (or alternatives like crun) as the low-level runtime.' }
    ],
    [
      { question: 'What is the default container runtime for Kubernetes 1.24+?', options: ['Docker', 'containerd', 'CRI-O', 'rkt'], answer: 1, explanation: 'containerd is the default runtime since Kubernetes 1.24 deprecated and removed Docker support.' },
      { question: 'What does the CRI acronym stand for?', options: ['Container Run Integration', 'Container Runtime Interface', 'Custom Runtime Integration', 'Container Registry Interface'], answer: 1, explanation: 'CRI stands for Container Runtime Interface — the gRPC API between kubelet and container runtimes.' },
      { question: 'Which two services does the CRI gRPC API define?', options: ['PodService and ContainerService', 'RuntimeService and ImageService', 'NetworkService and StorageService', 'ExecService and LogService'], answer: 1, explanation: 'CRI defines RuntimeService (Pod sandboxes and containers) and ImageService (image management).' },
      { question: 'What provides additional security isolation for multi-tenant containers?', options: ['Seccomp', 'AppArmor', 'gVisor or Kata Containers', 'Pod security policy'], answer: 2, explanation: 'gVisor and Kata Containers provide strong security isolation by running containers in user-space kernels or lightweight VMs.' },
      { question: 'Which command lists images in containerd?', options: ['docker images', 'ctr images list', 'crictl images', 'nerdctl images'], answer: 2, explanation: 'crictl is the CRI-compatible CLI for managing containers and images on a node. crictl images lists cached images.' },
      { question: 'What is the low-level OCI runtime used by both containerd and CRI-O?', options: ['Docker', 'runC', 'gVisor', 'Kata'], answer: 1, explanation: 'runC is the reference OCI runtime implementation used by both containerd and CRI-O for actual container execution.' },
      { question: 'Which filesystem technology does containerd use for image layers?', options: ['ext4', 'overlayfs', 'btrfs', 'ZFS'], answer: 1, explanation: 'containerd uses overlayfs (OverlayFS) as the default snapshot driver for efficient image layer management.' },
      { question: 'What is the port for containerd\'s CRI gRPC socket?', options: ['/var/run/docker.sock', '/var/run/containerd/containerd.sock', '/run/cri-o/crio.sock', '/var/run/cri.sock'], answer: 1, explanation: 'containerd listens on /var/run/containerd/containerd.sock for CRI gRPC requests from kubelet.' },
      { question: 'What Linux kernel feature limits container CPU usage?', options: ['namespaces', 'cgroups', 'seccomp', 'capabilities'], answer: 1, explanation: 'cgroups (control groups) limit and account for resource usage including CPU, memory, and I/O.' },
      { question: 'What is the purpose of the kubelet --container-runtime flag?', options: ['Select the network plugin', 'Select the container runtime endpoint', 'Select the storage driver', 'Select the proxy mode'], answer: 1, explanation: 'The --container-runtime flag specifies the CRI socket endpoint, e.g., remote for containerd or CRI-O.' }
    ],
    [
      { title: 'Check Container Runtime on a Node', useCase: 'Verify which runtime is installed', code: 'kubectl get nodes -o wide\nkubectl describe node <node-name> | findstr "Container Runtime"', description: 'Shows the container runtime version running on each node.' },
      { title: 'List Images with crictl', useCase: 'View cached container images on a node', code: 'crictl images\ncrictl image fsusage', description: 'Lists all cached container images and their disk usage (run on the node).' },
      { title: 'Inspect a Running Container', useCase: 'Debug container state and configuration', code: 'crictl ps\ncrictl inspect <container-id>', description: 'Lists running containers and inspects a specific container\'s details.' },
      { title: 'View containerd Logs', useCase: 'Troubleshoot image pull or runtime errors', code: 'sudo journalctl -u containerd --no-pager -n 100\ncrictl logs <container-id>', description: 'Shows containerd service logs for troubleshooting runtime issues.' },
      { title: 'Run a Container with crictl', useCase: 'Test runtime directly without Kubernetes', code: 'cat <<EOF | crictl runp -\n{\n  "metadata": {\n    "name": "test-sandbox"\n  }\n}\nEOF\ncrictl pods', description: 'Creates a Pod sandbox using crictl directly to test the runtime independently.' }
    ]
  )

];
